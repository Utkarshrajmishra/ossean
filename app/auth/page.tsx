import { AuthCard } from "@/components/AuthCard"

const Page=()=>{
    return(
        <div className="h-screen bg-midnight flex items-center justify-center w-full bg">
        <AuthCard/>
        </div>
    )
}

export default Page